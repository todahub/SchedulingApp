# Design Overview

この文書は、`awase-scheduler-next` の現時点の設計思想を整理するための内部向けメモです。  
README のような概要説明ではなく、プロダクトが何を解こうとしていて、なぜこの実装方針を採っているのかを共有することを目的とします。

現時点ではコードベースの読み取りに基づく整理を含みます。  
今後の開発で方針が明確になった箇所は、この文書を都度更新します。

## 1. このプロダクトが解く問題

このアプリは、友人同士や小規模グループのカジュアルな日程調整を、自然文コメントベースで扱えるようにすることを目指しています。

従来の日程調整ツールは、候補日ごとに `○ / △ / ×` を選ぶ構造が中心です。  
一方で実際の会話では、参加者は次のような曖昧で条件付きの表現を使います。

- `10日は夜なら行ける`
- `11日はたぶんいける`
- `12か13ならどっちか`
- `他のみんなが行けるなら自分も合わせる`

また、友人同士や小規模グループでは明確な幹事がいない場合が多いです。そのため
本プロダクトは、このようなコメントをできるだけそのまま受け取り、アプリが擬似的な幹事となり提案に変換することを狙います。

## 2. プロダクトの基本方針

### コメントを主入力にする

参加者の中心操作は、表形式の打鍵ではなくコメント入力です。  
日付選択 UI は入力補助であり、最終的な意味づけはコメント本文から読み取ります。

### 幹事の認知負荷を下げる

出力の最終目的は、全候補の一覧表示よりも、幹事が次に判断しやすい形へ整理することです。  
理想は「今いちばんまとまりやすい候補を 1 件提示し、必要なら代替候補や確認事項も示す」ことです。

### 賢さより再現性を優先する

自然文解釈は不確実性が高いため、全面的に LLM に任せません。  
まずルールベースで骨格を作り、その上で必要箇所だけ限定的に補完します。


## 3. 非目的

現時点では、次は主目的ではありません。

- 大規模組織向けの厳密な勤怠調整
- 分単位で完全に確定したスケジューリング
- LLM 単独で全解釈を完結させること
- 幹事の最終判断を完全自動化すること

このプロダクトは、厳密最適化エンジンというより、自然文ベースの調整補助システムとして設計します。

## 4. 想定ユーザーと役割

### イベント作成者

- イベントを作成する
- 候補日と時間帯の枠を決める
- 参加 URL を共有する

### 参加者

- 自分の都合を自然文で送る
- 必要に応じてカレンダー補助を使う
- 条件付き可否や希望をコメントで伝える

設計上は、イベント作成者の操作はできるだけ少なくし、参加者も会話感覚で入力できることを重視します。

## 5. システム全体像

大きな流れは次のとおりです。

1. イベント作成者がイベントと候補を作成する
2. 参加者がコメントを送信する
3. コメントから制約・条件・希望を抽出する
4. 候補ごとの参加可能性を整理する
5. アプリが擬似的な幹事として候補を比較し、提案を返す

この流れの中で、アプリの価値が最も大きいのは `3` から `5` です。  
イベント作成や共有は必要機能ですが、本質的な差別化ポイントは、コメント解釈をもとにアプリ自身が擬似幹事として整理と提案を担う点にあります。

## 6. 設計原則

### 6.1 ルールベースを中核に置く

コメント解釈の土台は、辞書・正規化・ルールベースです。  
これにより次が得られます。

- 同じ入力に対して同じ結果を返しやすい
- テストが書きやすい
- 誤作動時の原因を追いやすい
- 改善時に局所修正しやすい

### 6.2 LLM は補助役に限定する

LLM は便利ですが、出力揺れが大きく、全面依存するとプロダクトの挙動が不安定になります。  
そのため、現状では次のような局所タスクに寄せて使います。

- ラベル補完
- 対象と可否表現の関連付け補助
- 比較表現や条件文の解釈補助
- 複数 grouping 仮説の選択補助

### 6.3 説明可能な中間表現を残す

単に最終スコアを返すのではなく、途中で得たルール、制約、希望、条件、文脈を構造化して持ちます。  
これにより、結果画面で「なぜこの候補が上に来たか」を人間が追えるようにします。

### 6.4 UI と推論を分離する

UI は参加しやすさを担当し、解釈ロジックは `src/lib` に寄せます。  
見た目の調整と推論改善を独立して進めやすくするためです。

## 7. コメント解釈パイプライン

現時点の設計思想としては、コメント解釈は一発判定ではなく多段パイプラインです。

### 7.1 正規化

まず、改行、空白、全角半角、句読点ゆれなどを吸収します。  
ここでは「人間にとって同じ意味だが文字列として違う入力」を揃えます。

### 7.2 ラベル付け

コメント中のトークンや句に対して、日付、曜日、時間帯、可否、比較、例外、残余スコープなどのラベルを付けます。  
この段階は再現性重視で、基本はルールベースです。

### 7.3 target grouping

日付列挙や範囲表現は、どこまでを一塊の候補として解釈するかが難所です。  
そのため target group を組み、必要なら複数の grouping 仮説を持てるようにします。

例:

- `10, 11, 12 はいける`
- `10 と 11 は微妙、12 は行ける`
- `12か13ならどちらか`

これらは見た目が近くても構造が異なるため、grouping を明示的に扱います。

### 7.4 関係解決

ラベル付けだけでは、どの可否表現がどの target にかかるかが確定しません。  
そこで clause 単位や token index を使って、次の関係を解きます。

- どの可否がどの日付にかかるか
- どの条件がどの候補にかかるか
- どの例外句がどこを打ち消すか
- どの比較表現がどの候補群を比較しているか

### 7.5 条件・希望・比較の抽出

可否だけでなく、次も独立した情報として扱います。

- 条件付き可否
- 避けたい候補
- 積極的に希望する候補
- 他候補との比較に基づく選好
- 他人の参加状況に依存する条件

この層があることで、単純な `行ける / 無理` 以上の整理が可能になり、擬似幹事としての提案精度を上げられます。

### 7.6 候補単位への解決

最終的には、各候補日に対して参加者ごとの状態へ落とし込みます。

- 強く参加可能
- たぶん参加可能
- 条件付きで参加可能
- 未確定
- 避けたい
- 参加不可

### 7.7 fallback

コメントが空である、または安全に解釈できない場合は、無理に誤読せず default handling を行います。  
この安全策は、解釈精度が十分でない段階のプロダクトでは重要です。

## 8. ランキング思想

ランキングは単なる多数決ではありません。  
目指しているのは、アプリが擬似的な幹事として「今どの候補を推すべきか」を説明付きで返せる順に候補を並べることです。

現時点の理解では、少なくとも次の要素を加味しています。

- 参加可能人数
- 条件付き参加人数
- 不可人数
- hard no の有無
- コメント由来の制約
- 希望や回避の選好
- 比較表現から得られた優先度
- 条件が未解決のまま残っているか

このため、ランキングは単純なスコアリングというより、`参加可否 + 条件 + 希望 + 説明` をまとめ、暫定結論まで出すレイヤです。

## 9. 擬似幹事としての出力の考え方

このアプリの出力は、単なる集計画面ではありません。  
全情報を一度に見せるのではなく、まず結論を前に出し、必要なときだけ根拠へ潜れる構造を目指します。

理想の出力は次のような形です。

- 第一候補を 1 件提示する
- 代替候補を数件提示する
- 推した理由を短く添える
- 追加確認が必要な参加者や条件を示す
- 必要ならコメント解釈の根拠を展開できる

これは「AI が勝手に確定する」のではなく、「AI が擬似的な幹事として暫定結論と根拠を返す」ことを意図しています。

## 10. データと実行環境の設計

### repository abstraction

データ層は `demo` と `supabase` を切り替えられるようにします。  
この抽象化には次の利点があります。

- UI 開発と解釈ロジック開発を DB なしで進められる
- ローカル検証がしやすい
- 本番移行時に永続化層を差し替えやすい

### runtime configuration

実行時設定で repository mode と LLM provider を切り替えます。  
開発速度を落とさずに、本番構成へ近づけていくための設計です。

## 11. 品質方針

このプロジェクトでは、機能追加と同じくらい「壊さないこと」を重視します。  
特に自然文解釈は、1つの修正で別の表現を壊しやすいため、テスト戦略が設計の一部です。

現状の品質方針は次の通りです。

- regression test で過去ケースを守る
- guardrail test で危険な崩れ方を防ぐ
- integration test でパイプライン全体を見る
- visibility 系テストで中間生成物を観察できるようにする

これは、精度改善を継続的に積み上げるための土台です。

## 12. 現時点の未解決事項

まだ設計として固定しきれていない点もあります。

- 最終ランキングで `全会一致` と `最多参加` のどちらをどの状況で優先するか
- 条件付き可否をどこまで強く候補スコアへ反映するか
- 比較表現や希望表現を最終推薦文へどう要約するか
- 解釈不能コメントを UI 上でどう説明するか
- LLM の責務を今後どこまで拡張するか
- イベント作成者がアプリの提案を調整できる手動補正 UI を入れるか

## 13. 今後この文書に追記したいもの

次の項目は、設計がもう少し固まったら追加したいです。

- ドメインモデル一覧
- API 入出力仕様
- コメント解釈の詳細シーケンス図
- ランキング決定フロー
- fallback ポリシー一覧
- 将来の外部連携方針

## 14. 要約

このプロダクトは、自然文コメントによる日程調整を、再現性のある中間表現と限定的な LLM 補助で構造化し、アプリが擬似的な幹事として候補提案へ変換することを目的とします。

設計の芯は次の 3 点です。

- 参加者には自然に書かせる
- 内部では慎重に構造化する
- アプリが暫定結論と根拠を返す

この文書は初版なので、今後は実装の進展に合わせて、仮説だった部分を確定仕様へ寄せていきます。
