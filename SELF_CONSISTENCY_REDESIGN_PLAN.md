# Self-Consistency を用いた自然言語解釈再設計案

この文書は、自然言語コメントを JSON に変換し、その後にランキングへ落とす解釈パイプラインを見直すための設計案である。  
主眼は、`自然言語解釈の精度` と `システムとしての信頼性` を両立させることにある。

## 1. 再設計の前提

これまでの設計では、辞書やルールベースで先に主要語句を拾い、LLM の仕事量を減らすことで出力のブレを抑えようとしていた。  
しかし実際には、この方法は LLM の自然言語理解能力を削りやすく、特に比較、条件、弱い感情、消極的肯定のような自然文脈依存の表現で精度低下を招いた。

そのため、新設計では次を基本方針とする。

- 意味解釈そのものは LLM に自然文のまま行わせる
- ただし、すべてを一律に信じるのではなく、重要箇所だけ Self-Consistency で検証する
- `ぶれてはいけない部分` と `ぶれてよい部分` を分けて扱う
- 出力の信頼性は、前段の強い制約ではなく、後段の集約・検証・正規化で担保する

## 2. この設計で守りたいこと

このシステムでは、次のような解釈ミスは許されない。

- `11 は無理` を `11 は行ける` と解釈する
- `11 より 12 の方がよい` を `11 > 12` と逆向きに解釈する
- 条件が別の日付に誤って結びつく

一方で、次のような揺れは一定範囲で許容してよい。

- `11 は行きたいなあ` が強い希望か、やや弱い希望か
- `まあ行けなくもない` がどの程度弱い肯定か
- `12 の方がうれしい` が soft preference か strong preference か

つまり、今回の再設計では

- `極性や向き` は厳密に守る
- `感情や温度感` は分布として扱う

ことが重要になる。

## 3. 目指す全体像

新しい流れは次のようにする。

1. LLM で通常解釈を 1 回行う
2. 辞書で曖昧表現・比較表現・条件表現を検出する
3. リスクの高い部分だけ複数回推論する
4. 可否や比較の向きは多数決または一致率で扱う
5. 希望度や感情は分布または平均値で扱う
6. 最終的に JSON 化し、機械的にランキングする

この設計では、LLM に自然解釈をさせつつ、出力のうち `壊れてはいけない骨格` だけを局所的に安定化できる。

## 4. 実装しやすいアーキテクチャ

実装上は、解釈処理を大きく 5 レイヤに分けると扱いやすい。

### 4.1 Base Interpretation Layer

役割:

- コメント全文を自然文のまま LLM に解釈させる
- まずは 1 回だけ推論する
- 出力はランキング結果ではなく、意味中間表現の draft にする

ここでは、LLM の強みを最大化するために、前段で意味を固定しすぎない。  
出力は最低限の JSON にするが、比較的自然な意味項目を使う。

例:

- `targetEvaluations`
- `comparisons`
- `conditions`
- `meta`

### 4.2 Risk Detection Layer

役割:

- 辞書やルールベースで、追加検証が必要な箇所を見つける
- Self-Consistency をかける対象を限定する

この層で見るべきもの:

- 比較表現
- 条件表現
- 仮定表現
- 不確実表現
- 否定・二重否定
- 複数 target が近接している表現
- base interpretation で confidence が低い箇所

ここでの辞書は、意味を決めるためではなく `再推論対象の検出器` として使う。

### 4.3 Local Self-Consistency Layer

役割:

- リスクが高い部分だけ複数回推論する
- 全文を何度も回すのではなく、局所化した再解釈を行う

推論単位の候補:

- 句単位
- 節単位
- 比較句単位
- 条件句単位
- 特定 target を含む文単位

この層では、全文の文脈を補助情報として持たせつつ、主タスクは局所部分の安定確認に限定する。

### 4.4 Aggregation Layer

役割:

- 複数回推論の結果を集約する
- `ぶれてはいけない部分` と `ぶれてよい部分` で集約方法を変える

### 4.5 Canonicalization and Ranking Layer

役割:

- 集約済み意味表現を canonical JSON へ落とす
- target を候補日に展開する
- スコアリングしてランキングを作る

この層は完全に機械的に処理する。

## 5. Self-Consistency を局所的に適用する方法

全文に対して毎回複数推論をかけるとコストが高く、また不要なばらつきも増える。  
そのため Self-Consistency は局所的に適用する。

### 5.1 局所適用の対象

Self-Consistency を適用する候補は次の通り。

- 比較表現を含む節
- 条件表現を含む節
- 不確実・曖昧表現を含む節
- 否定や二重否定を含む節
- 一つの節に複数 target が含まれる箇所
- base interpretation で低 confidence の箇所

### 5.2 推論単位

推奨は `全文 + 焦点節` の組み合わせである。

例:

- 全文は参考文脈として渡す
- 焦点は `11 と 12 なら 12 かな` の比較節だけにする
- 出力はその節に関係する比較・可否・条件だけ返させる

これにより、自然文の流れを保ちながら、再推論対象を小さくできる。

### 5.3 推論回数

最初は固定 3 回が現実的である。  
その上で一致率が低い場合のみ 5 回まで増やす段階方式がよい。

例:

- 通常は 3 回
- 一致率が閾値未満なら追加 2 回
- それでも割れる場合は unresolved 扱い

## 6. 希望度の集約方法

希望度や感情は、可否のように単純な多数決だけでは扱いにくい。  
そのため、離散ラベルを数値へ写像して分布として保持するのが実装しやすい。

### 6.1 推奨ラベル

- `strong_avoid`
- `avoid`
- `neutral`
- `prefer`
- `strong_prefer`

### 6.2 数値写像

例:

- `strong_avoid = -2`
- `avoid = -1`
- `neutral = 0`
- `prefer = 1`
- `strong_prefer = 2`

### 6.3 集約値

保持するとよい値:

- 平均値
- 標準偏差または分散
- ラベルごとのヒストグラム
- 最頻値

これにより、

- 平均が高ければ好みが強い
- 分散が大きければ解釈が揺れている

と扱える。

### 6.4 実運用上の使い方

ランキングに直接使う主値:

- 平均値

説明表示に使う補助値:

- ヒストグラム
- 分散
- 最頻値

こうすると、最終順位は滑らかに扱いつつ、内部では揺れも保存できる。

## 7. 可否判定の信頼度計算方法

可否は感情より厳密に扱う必要があるため、まず `極性` を守ることを優先する。

### 7.1 可否ラベル

- `hard_no`
- `soft_no`
- `unknown`
- `soft_yes`
- `strong_yes`

### 7.2 基本集約

第一段階では、多数決よりも `極性の一致` を見る。

例:

- `hard_no / soft_no` は負極性
- `soft_yes / strong_yes` は正極性
- `unknown` は保留

### 7.3 推奨する信頼度指標

最低限持つとよい値:

- `topLabelAgreement = 最頻ラベル数 / 試行回数`
- `polarityAgreement = 最頻極性数 / 試行回数`
- `oppositePolarityRate = 反対極性が出た割合`

### 7.4 実際の判定ルール

例:

- すべて `hard_no` なら高信頼 `hard_no`
- `hard_no` と `soft_no` に割れても、正極性が 0 なら負極性として採用可
- `soft_yes` と `strong_yes` に割れても、負極性が 0 なら正極性として採用可
- 正負が混在したら unresolved または再推論対象

### 7.5 比較の向きも同様に扱う

`11 < 12` と `12 < 11` のような比較方向は、可否と同じく壊れてはいけない情報なので、方向一致率を信頼度として扱う。

## 8. 推論コストを抑える方法

Self-Consistency を導入すると、最も問題になるのは API コストとレイテンシである。  
そのため、設計時点でコスト制御を入れておくべきである。

### 8.1 全文複数回推論を避ける

全文を 5 回回すのではなく、

- 通常は全文 1 回
- 必要箇所だけ局所 3 回

とする。

### 8.2 Adaptive Sampling

最初から 5 回回さず、3 回で一致したら打ち切る。

### 8.3 キャッシュ

次をキャッシュ対象にするとよい。

- 正規化済みコメント
- base interpretation
- 局所 clause ごとの再推論結果

同一コメントの再計算を避けられる。

### 8.4 リスクベース実行

辞書上、比較語・条件語・曖昧語がない場合は Self-Consistency を走らせない。

例:

- `11 は無理`
- `12 は行ける`

のような単純文は base interpretation + 軽い検証だけで済ませられる。

### 8.5 集約対象の絞り込み

感情や希望は全候補に対して無差別に評価せず、LLM が実際に言及した target だけに適用する。

## 9. 既存コードの残し方

変更前コードは一応残す前提なので、`旧解釈パイプライン` と明示して分離した方がよい。

### 9.1 推奨ディレクトリ方針

新設計導入時に、旧コードは例えば次のように整理する。

- `src/lib/legacy-interpretation/`

この中に、現在の LLM + 辞書統合系をまとめる。

候補:

- `availability-comment-interpretation.ts`
- `availability-comment-interpretation-server.ts`
- `availability-comment-interpretation-prompt.ts`
- `availability-interpretation.ts`
- `comparison-preference-interpretation.ts`
- `condition-interpretation.ts`
- `comment-labeler/`
- `date-sequence/`

### 9.2 命名方針

ファイル移動が大きすぎる場合は、まずはコメントと index だけでも明示する。

例:

- `legacy-interpretation-index.md`
- `legacy-interpretation.ts`
- `// legacy pipeline before self-consistency redesign`

### 9.3 新設計用の推奨構成

新しい解釈系は別ディレクトリに分けると見通しが良い。

例:

- `src/lib/self-consistency/`
  - `base-interpretation.ts`
  - `risk-detection.ts`
  - `local-consistency.ts`
  - `aggregation.ts`
  - `confidence.ts`
  - `canonicalization.ts`
  - `pipeline.ts`
  - `types.ts`

これにより、旧方式と新方式を混在させず比較しやすくなる。

## 10. 推奨する最初の実装順

実装順は次のように刻むと安全である。

1. base interpretation の JSON schema を定義する
2. risk detection を辞書ベースで作る
3. clause 単位の local self-consistency を作る
4. availability / comparison だけ先に集約する
5. preference distribution を後から足す
6. 既存 ranking へ接続する

特に最初は、

- 可否の向き
- 比較の向き

だけを Self-Consistency の対象にすると、価値が分かりやすい。

## 11. まとめ

今回の再設計のポイントは、LLM を全面的に縛るのではなく、`壊れてはいけない情報だけを局所的に安定化する` ことにある。

設計としては次が中心になる。

- 全文 1 回の自然解釈
- 辞書によるリスク箇所の検出
- 局所的な Self-Consistency
- 可否は一致率重視
- 希望度は分布として保持
- 後段で機械的に JSON 化・ランキング化

この構成であれば、自然言語の感情や希望の揺らぎを捨てずに、システムとしての信頼性も高めやすい。
